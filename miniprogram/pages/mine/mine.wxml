<!-- pages/mine/mine.wxml -->
<view class="page">

  <!-- ── 个人信息 Banner ── -->
  <view class="profile-banner">
    <button class="avatar-btn" open-type="chooseAvatar" bindchooseavatar="onChooseAvatar">
      <image wx:if="{{avatarUrl}}" class="avatar-img" src="{{avatarUrl}}" mode="aspectFill"/>
      <view wx:else class="avatar-empty">
        <text class="avatar-empty-icon">👤</text>
        <text class="avatar-hint">点击头像</text>
      </view>
    </button>
    <input
      class="nickname-input"
      type="nickname"
      placeholder="点击设置昵称"
      placeholder-style="color:rgba(255,255,255,0.4);font-size:30rpx;font-weight:600"
      value="{{nickName}}"
      bindinput="onNickNameInput"
    />
  </view>

  <!-- ── 功能菜单 ── -->
  <view class="menu-section">
    <view class="menu-item" bindtap="goWatchlist">
      <view class="menu-left">
        <text class="menu-icon">⭐</text>
        <text class="menu-label">我的自选</text>
      </view>
      <view class="menu-right">
        <text class="menu-count" wx:if="{{favCount>0}}">{{favCount}}只</text>
        <text class="menu-arrow">›</text>
      </view>
    </view>
    <view class="menu-item" bindtap="goExport">
      <view class="menu-left">
        <text class="menu-icon">📊</text>
        <text class="menu-label">导出数据图</text>
      </view>
      <view class="menu-right">
        <text class="menu-sub">生成4张数据图</text>
        <text class="menu-arrow">›</text>
      </view>
    </view>
  </view>

  <!-- ── 免责声明 ── -->
  <view class="disclaimer">
    <text class="disclaimer-text">本应用数据仅供参考，不构成任何投资建议。投资有风险，入市须谨慎。</text>
  </view>

  <view style="height:120rpx"/>
</view>
