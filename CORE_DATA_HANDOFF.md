# Wise ETF 核心数据链路交接

快照日期：2026-08-20

本文件只覆盖三项核心业务：每日展示、场外申购额度日更、场内 ETF 溢价日更。QDII 估值和小程序业务不在本轮范围。

## 当前结论

正常上游条件下，三类基金和 ETF 可以生成完整快照；但当前实现尚不能保证“每天一定更新成功且不会回退旧数据”。

## 必修问题（按顺序）

1. **禁止日更字段倒退**
   - `_fetch_daily_snapshot` 只能用 fallback 补齐缺失的 value/as_of 原子组。
   - 较旧 fallback 不得覆盖较新的 BasicInformation 数据。
   - 基金和 ETF 合并 LKG 时，必须按字段组比较 `as_of`；旧候选不能覆盖新快照。

2. **让额度日更独立成功**
   - 申购状态与额度来自 `FundMNBasicInformation`，失败时显示“待确认”，不得冒用旧额度为 fresh。
   - `subscription_as_of` 必须按北京时间校验新鲜度。
   - 跟踪误差失败不能阻止当天额度、净值和滚动收益持久化。

3. **只在收盘后发布正式 ETF 溢价**
   - 公共 `/api/etfs` 冷缓存请求不得把盘中行情发布为每日正式快照。
   - 正式快照应由受保护的收盘 cron 发布。
   - 校验 A 股交易日、收盘时间和 `quote_as_of`；节假日及旧报价只能标记 stale。
   - 继续保证 quote 与 NAV 任一侧缺失时不跨轮重算 premium。

4. **修正 cron 的 30 秒预算**
   - `/api/cron/refresh` 当前顺序执行三类基金，每类最长等待约 20 秒，可能超过 Vercel 30 秒限制。
   - 拆分分类任务，或使用全局 25 秒 deadline；响应返回后不得继续后台写入。
   - Redis 发布失败必须让 cron 返回非 2xx 或明确失败状态，不能吞错后仍显示成功。

5. **修正缓存新鲜度与并发发布**
   - 36 小时缓存可用于兜底，但跨交易日必须变成 stale，不能继续显示 fresh。
   - 将全量数据集 LKG 改为按产品、按字段组单调更新。
   - 多个 Vercel 实例使用 Redis 分布式锁和幂等 run id；较旧任务晚完成时不得覆盖较新任务。

## 最低验收条件

- Basic 为 8 月 20 日、fallback 为 8 月 18 日时，保留 Basic 的 NAV/滚动收益，fallback 只补缺失字段。
- 当天额度抓取失败时返回 `unknown/待确认` 或带日期的 stale，不把昨日额度标 fresh。
- 工作日 10:30 的 ETF 行情不能生成 fresh premium 或写入 LKG；收盘后有效行情才可发布。
- 任意一只跟踪误差失败时，其余产品当天的净值、额度和溢价仍可持久化。
- 三类基金和 ETF 的 cron 均在 Vercel 30 秒限制内结束，并记录发布确认、覆盖率和失败分类。
- 跨日旧缓存、旧 writer 晚完成、Redis 写失败、上游超时都有回归测试。

## 预计工时

- 只修上述核心链路并补回归测试：约 6–8 小时。
- 加上部署 smoke、真实 cron 验证和一天观察窗口：约 1–2 个工作日。
- 若同时拆分整个 `api/index.py`：建议另行安排，不与核心修复一起扩大范围。
